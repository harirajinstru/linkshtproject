# Agent Instructions — Operations & scripts

Scope
- How to run and verify common project tasks and scripts.

Common commands
- Start dev server: `npm run dev`
- Build: `npm run build`
- Start production server: `npm start`
- Lint: `npm run lint`

Environment
- Document required env vars in `README.md` and avoid hardcoding values.

Deploy notes
- This project targets Next.js — deployment should follow the host's recommended process. Any infra changes require human review.
